apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-claim
  namespace: default
spec:
  volumeName: minio-pv
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: ${volumeSize}
  storageClassName: ${storageClassName}
  volumeMode: Filesystem
